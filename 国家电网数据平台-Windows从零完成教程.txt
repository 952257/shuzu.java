国家电网数据平台
Windows 环境从头到尾完成教程
企业开发项目  +  企业运维项目
对应代码：springboot \ State-Grid
统一密码 root / 127307
写在前面：这份教程带你做什么
试卷有两份 Word：企业开发项目、企业运维项目。本教程按 Windows 电脑（Windows 10/11 + IntelliJ IDEA）从零做到能交差。
开发部分：在 IDEA 里跑两个 Spring Boot，完成拿 Token、同步用户台账、生成 Excel、导出 Word 日报。代码已经写在 springboot\State-Grid 里，你主要是部署、跑通、截图。
运维部分：在 Windows 上用 Docker Desktop 完成 MySQL 主从、Redis 哨兵、Nginx、Prometheus+Grafana；再用客户端做 EMP/DEPT 的 SQL 题。如果老师强制要求 CentOS 7 虚拟机，见第六章。
注意：开发接口必须打 8080（state-grid-app），不要打 18080。18080 只是模拟第三方 Blade。H2 控制台不要用默认的 jdbc:h2:~/test。
一、项目到底要交什么
1. 企业开发项目
依据 yml 里配置的租户、账号、密码、clientId/clientSecret，调用远程 OAuth 拿 Token，并提供查询接口返回 Token。
用 Token 里的 accessToken 调用用户信息台账分页接口。定时任务启动时跑一次、之后每天 03:00 再跑，增量同步到本地表，同时生成 Excel。
提供日报导出：GET /report/export?date={date}，返回 Word，格式为「xxxx年 xx月 xx日 台账统计报告 / 新增账户 / 已删除账户」。
2. 企业运维项目
安装 CentOS7（或用 Windows 上的 Docker 等价完成）、Docker、Docker Compose；MySQL 主从热备份；Redis 哨兵；Nginx 反向代理负载均衡；Prometheus+Grafana 监控 Spring Boot；按题目建 EMP/DEPT 并写 SQL。
二、Windows 环境准备
1. 必装软件
JDK 安装完后，系统环境变量要有 JAVA_HOME，例如 C:\Program Files\Eclipse Adoptium\jdk-17，Path 里加上 %JAVA_HOME%\bin。命令提示符执行：
java -version
mvn -version
应能看到 java version "17..."。如果提示 mvn 不是内部或外部命令，把 Maven 的 bin 目录也加到 Path。
2. IDEA 打开工程
用 IDEA 打开已有工程根目录，确保 springboot\pom.xml 的 modules 里包含 State-Grid。
右侧 Maven 面板点 Reload All Maven Projects。能看到：
springboot
  └─ State-Grid
       ├─ blade-mock
       └─ state-grid-app
如果 State-Grid 是红的，先确认父 pom 已写入 <module>State-Grid</module>，再 Reload。
3. 两个模块分别干什么
yml 里远程地址默认指向本机 mock，所以不必真去搭 Blade 官方工程。账号如下（已写进 application.yml，不用改）：
三、开发项目：部署与跑通
1. 业务流程图（演示前先看懂）
yml 账号
   |
   v
GET http://127.0.0.1:8080/auth/token     <-- 试卷接口，浏览器/Apifox 打这个
   |
   v
POST http://127.0.0.1:18080/blade-auth/token   <-- app 内部去调 mock
   |  Header: Authorization Basic(saber:saber_secret)
   v
GET  http://127.0.0.1:18080/blade-user/user-list?current=1&size=10
   |  Header: Blade-Auth = accessToken
   v
增量写入 H2 表 LOCAL_USER_LEDGER
统计写入 SYNC_REPORT
生成 Excel 到 state-grid-app\data\excel\
   v
GET http://127.0.0.1:8080/report/export?date=yyyy-MM-dd  <-- 下载 Word
定时：程序启动立刻同步一次；之后每天凌晨 03:00 再同步。演示时用 POST /job/sync 立刻再跑一遍即可。
2. 编译
在 IDEA 底部 Terminal（或 Windows 的 cmd / PowerShell）进入目录打包：
cd springboot\State-Grid
mvn -DskipTests package
成功后有两个 jar：
blade-mock\target\blade-mock-1.0-SNAPSHOT.jar
state-grid-app\target\state-grid-app-1.0-SNAPSHOT.jar
注意：本机若是 JDK 21，Lombok 已在 pom 里指定 1.18.34，一般可直接编过。仍报错时把 IDEA 的 Project SDK 改成 17 再编。
3. 启动（顺序不能反）
方式 A：IDEA 图形界面（推荐）
1）打开 BladeMockApplication，点绿色三角 Run。控制台出现 Tomcat started on port(s): 18080 再进行下一步。
2）打开 StateGridApplication，再点 Run。出现 Tomcat started on port(s): 8080。同时会打印「应用启动，立即执行一次用户台账同步」。
方式 B：两个命令行窗口
cd springboot\State-Grid\blade-mock
java -jar target\blade-mock-1.0-SNAPSHOT.jar
cd springboot\State-Grid\state-grid-app
java -jar target\state-grid-app-1.0-SNAPSHOT.jar
注意：第二个命令的当前目录必须是 state-grid-app，否则 H2 文件路径会错。Windows 不要关这两个窗口。
4. 用浏览器 / PowerShell 验收开发接口
PowerShell 里执行（把日期改成当天）：
curl http://127.0.0.1:8080/actuator/health
curl http://127.0.0.1:8080/auth/token
curl -Method POST http://127.0.0.1:8080/job/sync
curl http://127.0.0.1:8080/report/stat?date=2026-09-04
curl -L -o $env:USERPROFILE\Desktop\台账统计报告.docx http://127.0.0.1:8080/report/export?date=2026-09-04
Word 打开后应类似：
2026年 9月 4日 台账统计报告
新增账户：_10__个
已删除账户：_1__个
国家电网数据平台
5. Apifox 导入与测试
1）打开 Apifox → 导入 → Postman。
2）选择 springboot\State-Grid\apifox\state-grid.postman_collection.json。
3）再导入同目录 state-grid.postman_environment.json。
4）环境变量 baseUrl 必须是 http://127.0.0.1:8080 ，不要末尾斜杠，不要写成 18080。
5）按文件夹顺序跑：01 获取 Token → 02 台账同步 → 03 导出 Word。05 才是打 18080 的第三方模拟。
注意：若出现 404 且 path 为 //auth/token，就是 baseUrl 多了斜杠或端口用成了 18080。改环境变量后重发即可，不是代码坏了。
6. 用 H2 控制台看库
浏览器打开（末尾斜杠不能省）：
http://127.0.0.1:8080/h2-console/
登录不要用页面默认的 jdbc:h2:~/test（会报 Database not found）。Windows 路径用正斜杠：
JDBC URL:
jdbc:h2:file:C:/你的路径/springboot/State-Grid/state-grid-app/data/state_grid;MODE=MySQL;AUTO_SERVER=TRUE

User Name: sa
Password: （空）
例如工程在 D:\IdeaProjects\javafirst 下：
jdbc:h2:file:D:/IdeaProjects/javafirst/springboot/State-Grid/state-grid-app/data/state_grid;MODE=MySQL;AUTO_SERVER=TRUE
Connect 之后应看到两张表：LOCAL_USER_LEDGER（用户台账）、SYNC_REPORT（当天新增/删除统计）。
7. 演示增量同步（答辩加分）
在 PowerShell：
curl.exe -X POST http://127.0.0.1:18080/blade-user/mock/add -d "account=newuser01&name=新员工"
curl.exe -X POST http://127.0.0.1:18080/blade-user/mock/delete/10002
curl.exe -X POST http://127.0.0.1:8080/job/sync
第二次同步的 newCount 会加 1，deletedCount 会加 1。再导出一次 Word，两个数字会对上。Excel 在：
springboot\State-Grid\state-grid-app\data\excel\user-ledger-当天日期.xlsx
8. 开发部分常见问题
四、运维项目：Windows + Docker Desktop
考试原文写的是克隆 CentOS 7。在 Windows 上更稳的做法是安装 Docker Desktop，用容器完成同等拓扑，截图交文档。若机房强制虚拟机，见第六章。
1. 安装 Docker Desktop
1）官网下载 Docker Desktop for Windows，安装时勾选 Use WSL 2。
2）重启电脑，启动 Docker Desktop，托盘图标不再转圈。
3）PowerShell 执行 docker version 和 docker compose version，都能输出版本号。
资源建议：Settings → Resources，内存至少 4GB（8GB 更稳）。
2. 启动中间件
若仓库有 springboot\State-Grid\docker\docker-compose.yml，直接：
cd springboot\State-Grid\docker
docker compose up -d
docker compose ps
需要起来的服务与端口：
全部 Up 后再做检查。第一次拉镜像需要联网，等几分钟。
3. MySQL 主从验证
docker exec -it sg-mysql-slave mysql -uroot -p127307 -e "SHOW REPLICA STATUS\G"
看 Replica_IO_Running、Replica_SQL_Running 都是 Yes。再在主库建个测试库，从库能查到即同步成功。也可用 Navicat 连 127.0.0.1:3306（主）和 3307（从）。
4. Redis 哨兵验证
docker exec -it sg-redis-sentinel1 redis-cli -p 26379 SENTINEL masters
能看到 mymaster。故障转移演示：docker stop 主容器，等约 5 秒再查 get-master-addr-by-name，会切到从库。
5. Nginx 反向代理
两个 Spring Boot 保持在 IDEA 里跑。Nginx 把 80 端口转到 8080，并把 /blade-auth/、/blade-user/ 转到 18080。浏览器访问：
http://127.0.0.1/auth/token
若要演示负载均衡，IDEA 再起一份 state-grid-app，Program arguments 填 --spring.profiles.active=replica（端口 8081）。
注意：Windows 上 80 端口有时被 IIS 占用。占用时把映射改成 8088:80，访问 http://127.0.0.1:8088 。
6. Prometheus + Grafana
应用已暴露 http://127.0.0.1:8080/actuator/prometheus 。
Prometheus：http://127.0.0.1:9090 → Status → Targets，job=state-grid-app 为 UP。
Grafana：http://127.0.0.1:3000 ，账号 admin，密码 127307。数据源选 Prometheus。可导入看板 ID 4701（JVM）。截图：应用 UP、堆内存曲线。
7. MinIO
控制台 http://127.0.0.1:9001 ，Access Key=remote_user，Secret Key=0123456789.abcdefg，Bucket=demo。当前精简开发版 Excel 写在本地 data\excel，答辩时说明即可。
五、运维 SQL 题（直接可交）
用客户端连 MySQL（Docker 主库 3306 或本机 MySQL），新建库 ops_demo，执行下面全文，保存为 ops-emp-dept.sql 一并提交。
CREATE DATABASE IF NOT EXISTS ops_demo DEFAULT CHARACTER SET utf8mb4;
USE ops_demo;

DROP TABLE IF EXISTS EMP;
DROP TABLE IF EXISTS DEPT;

CREATE TABLE DEPT (
  ID BIGINT PRIMARY KEY,
  NAME VARCHAR(20) NOT NULL,
  LOCATION VARCHAR(100)
);

CREATE TABLE EMP (
  ID BIGINT PRIMARY KEY,
  NAME VARCHAR(100) NOT NULL,
  SALARY INT,
  DEPTID INT,
  CONSTRAINT fk_emp_dept FOREIGN KEY (DEPTID) REFERENCES DEPT(ID)
);

INSERT INTO DEPT VALUES (1,'研发部','一楼'),(2,'人事部','二楼');
INSERT INTO EMP VALUES (1001,'张三',5000,1),(1002,'李四',4000,1),(1003,'王五',1000,2),(1004,'赵六',3000,2);

-- 1 张三薪水提升 10%
UPDATE EMP SET SALARY = SALARY * 1.1 WHERE NAME = '张三';

-- 2 姓王的员工按薪水降序
SELECT * FROM EMP WHERE NAME LIKE '王%' ORDER BY SALARY DESC;

-- 3 第 2 到 4 条记录
SELECT * FROM EMP LIMIT 1, 3;

-- 4 左外连接员工和部门
SELECT e.*, d.NAME AS DEPT_NAME, d.LOCATION
FROM EMP e LEFT JOIN DEPT d ON e.DEPTID = d.ID;

-- 5 各部门人数
SELECT d.NAME, COUNT(e.ID) AS CNT
FROM DEPT d LEFT JOIN EMP e ON d.ID = e.DEPTID
GROUP BY d.ID, d.NAME;

-- 6 平均薪水大于 3000 的部门
SELECT d.NAME, AVG(e.SALARY) AS AVG_SALARY
FROM EMP e JOIN DEPT d ON e.DEPTID = d.ID
GROUP BY d.ID, d.NAME
HAVING AVG(e.SALARY) > 3000;
六、老师点名要 CentOS 7 虚拟机时（可选）
Windows 推荐 VMware Workstation。CentOS 7 选 Minimal 或 DVD ISO。root 密码设为 127307。
本机内存建议 16GB 以上。开 2 台即可：
安装完进入系统：systemctl stop firewalld；yum 安装 docker 与 docker-compose；把项目拷进去再 compose up。两台用桥接或仅主机网络，互相能 ping。
注意：笔记本内存小于 8GB 时不要强开两台虚拟机，用第四章 Docker Desktop 即可，答辩说明「Windows 上用容器等价部署」。
七、答辩演示建议顺序（约 15 分钟）
1. IDEA 展示两个启动类，先 mock 后 app。
2. 浏览器打开 /auth/token，指出 accessToken。
3. Apifox 跑 /job/sync，H2 打开 LOCAL_USER_LEDGER。
4. 下载 Word 日报，对照新增/删除数字。
5. 若运维已起：Docker Desktop 窗口、从库 SHOW REPLICA STATUS、Grafana 看板、Nginx 访问 80 端口。
6. 客户端跑第五章 SQL，展示查询结果。
八、打包提交
开发：选中 springboot\State-Grid，排除 target、data、.idea，右键压缩为 State-Grid-src.zip，附上 sql\schema.sql。
运维：本 Word 可作四份安装文档的合并版；SQL 另存 ops-emp-dept.sql。按老师要求拆成四个 txt 也可以，内容从第四章对应小节复制。
源码不要提交 H2 的 data 目录和 Excel 运行产物。
附录：端口一览
做到这里：开发两个进程能跑、四个接口能通、H2 有数据、Word 能下载；运维容器能起、SQL 能跑，即可按试卷要求交付。

交付物 | 本仓库对应位置
源码 zip | 把 springboot\State-Grid 打成 zip（不要含 target、data）
数据库 sql（只要结构） | sql\schema.sql

交付物 | 怎么交
MySQL 主从安装文档 | 本教程第四章可直接当文档
Redis 哨兵安装文档 | 本教程第四章
Nginx 安装文档 | 本教程第四章
Prometheus+Grafana 安装文档 | 本教程第四章
数据库 SQL 脚本 | 本教程第五章，保存为 ops-emp-dept.sql

软件 | 建议版本 | 用途
JDK | 17（Temurin 或 Oracle） | 编译运行 Spring Boot
Maven | 3.8 / 3.9 | 打包
IntelliJ IDEA | 2023 及以上 | 打开工程、启动两个应用
Apifox | 最新版 | 导入并测接口
Git（可选） | 最新版 | 拉代码
Docker Desktop（运维用） | 最新稳定版 | MySQL/Redis/Nginx/监控

模块 | 启动类 | 端口 | 角色
blade-mock | com.blademock.BladeMockApplication | 18080 | 假装第三方 Blade：发 Token、给用户台账
state-grid-app | com.stategrid.StateGridApplication | 8080 | 你要交的开发程序：拿 Token、同步、导出 Word

配置项 | 值
clientId | saber
clientSecret | saber_secret
tenantId | 000000
username | admin
password | 21232f297a57a5a743894a0e4a801fc3（admin 的 MD5）

接口 | 成功长什么样
/actuator/health | status 为 UP，database 为 H2
/auth/token | success true，data.accessToken 有一串字符
/job/sync | totalCount 至少为 1
/report/export | 桌面上出现可打开的 docx

现象 | 原因 | 处理
app 启动提示连不上 18080 | mock 没先启动 | 先起 blade-mock 再起 app，或再点一次 /job/sync
8080 端口占用 | 上次 Java 没关 | 任务管理器结束 java.exe
H2 404 | 打开了 /h2-console 没斜杠 | 改用 /h2-console/
H2 Database not found | 用了默认 ~/test | 换成项目 data\state_grid 的绝对路径
Apifox 404 | 打到 18080 或双斜杠 | baseUrl=http://127.0.0.1:8080

服务 | 端口 | 账号
MySQL 主 | 3306 | root / 127307
MySQL 从 | 3307 | root / 127307
Redis 主 | 6379 | 密码 127307
Redis 从 / 哨兵 | 6380、6381 / 26379-26381 | 哨兵名 mymaster
Nginx | 80 | 反代 8080 与 18080
MinIO | 9000 / 9001 | remote_user / 0123456789.abcdefg，bucket=demo
Prometheus | 9090 | 抓取 /actuator/prometheus
Grafana | 3000 | admin / 127307

虚拟机 | CPU | 内存 | 磁盘 | 角色
vm1 | 2 核 | 4GB | 40GB | Docker、MySQL 主、Nginx、监控、跑应用
vm2 | 2 核 | 4GB | 30GB | MySQL 从、Redis 哨兵整套

端口 | 谁在听 | 你怎么用
18080 | blade-mock | 只测 /blade-auth/token、/blade-user/user-list
8080 | state-grid-app | 试卷接口、H2、actuator
8081 | app 第二实例 | 可选，负载均衡
3306 / 3307 | MySQL 主/从 | 运维
6379 等 | Redis / 哨兵 | 运维
80 | Nginx | 运维反代
9000 / 9001 | MinIO | 对象存储控制台
9090 / 3000 | Prometheus / Grafana | 监控