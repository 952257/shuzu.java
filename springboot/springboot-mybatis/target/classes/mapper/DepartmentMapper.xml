<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.springboot.mybatis.mapper.DepartmentMapper">

    <resultMap id="departmentAndEmployeeMap" type="Department">
        <id property="id" column="id"/>
        <result property="name" column="name"/>
        <result property="location" column="location"/>
        <collection property="emps" ofType="Employee"
         select="com.springboot.mybatis.mapper.EmployeeMapper.selectEmployeeByDepartmentId"
         column="id"
        />
    </resultMap>

    <select id="selectDepartmentById" resultMap="departmentAndEmployeeMap">
        select * from t_department where id = #{id}
    </select>

    <resultMap id="departmentAndEmployeeMap2" type="Department">
        <id property="id" column="dept_id"/>
        <result property="name" column="dept_name"/>
        <result property="location" column="location"/>
        <collection property="emps" ofType="Employee">
            <id property="id" column="emp_id"/>
            <result property="name" column="emp_name"/>
            <result property="salary" column="salary"/>
        </collection>

    </resultMap>

    <select id="selectDepartmentById2" resultMap="departmentAndEmployeeMap2">
        select d.id as dept_id,d.name as dept_name,
               d.location,e.id as emp_id,
               e.name as emp_name,
               e.salary
        from t_department d left join t_employees e on d.id = e.dept_id
        where d.id = #{id}
    </select>
</mapper>